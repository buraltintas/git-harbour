export type Coord={x:number;y:number};
export type Day={date:string;weekday:number;contributionCount:number;contributionLevel:number};
export type FleetCellState='unknown'|'miss'|'empty'|'eligible'|'deployed'|'reserve'|'exposed'|'eliminated'|'selected'|'locked';
export type FleetCell=Coord&{state:FleetCellState;date?:string;weekday?:number;contributionCount?:number;contributionLevel?:number;dayPower?:number;combatLevel?:number;unitKind?:'contribution'|'reserve';unitPower?:number;targeted?:boolean};
export type BattleEvent={actor:'player'|'ai';x:number;y:number;result:'hit'|'miss'};
export type DeploymentChoice=Coord&{kind:'contribution'|'reserve'};
export type Stats={games:number;wins:number;losses:number;rating:number;shots:number;hits:number;currentStreak:number;longestStreak:number;winRate:number;accuracy:number;averageShotsPerWin:number;rank:string};
export type Period={start:string;end:string};
export type WindowSummary={totalContributions:number;activeDays:number;contributionPower:number;fleetCapacity:number;peakCount:number;peakDate:string;maxDeployedPower:number};
export type Game={id:string;mode:'solo'|'pvp';ruleset:'contribution_battleship_v4';status:'deployment'|'battle'|'complete';currentTurn:'setup'|'player'|'ai'|'complete';winner?:'player'|'ai';playerCells:FleetCell[];enemyCells:FleetCell[];playerFleetCapacity:number;enemyFleetCapacity:number;playerUnitsAlive:number;enemyUnitsAlive:number;playerSummary:WindowSummary;enemySummary?:WindowSummary;turns:number;shots:number;hits:number;misses:number;aiShots:number;aiHits:number;aiMisses:number;playerPeriod:Period;enemyPeriod?:Period;ratingDelta?:number;shareId?:string;stats:Stats;recentShots?:BattleEvent[];role?:'challenger'|'defender';automatedDefense?:boolean;challenger?:PublicIdentity;defender?:PublicIdentity;opponent?:PublicIdentity};
export type User={login:string;name:string;avatarUrl:string;joinedAt:string;solo:Stats;pvp:Stats;publicProfileUrl:string};
export type PublicUser={login:string;name:string;avatarUrl:string;joinedAt:string;solo:Stats;pvp:Stats;publicContributionSummary:{total:number;activeDays:number;preview:Day[]};pvpHistory?:PvpHistoryItem[]};
export type PublicIdentity={login:string;name:string;avatarUrl:string;pvp?:Stats};
export type ChallengeStatus='open'|'accepted'|'ready'|'battle'|'complete'|'cancelled'|'expired';
export type Challenge={code:string;status:ChallengeStatus;creator:PublicIdentity;opponent?:PublicIdentity;creatorReady:boolean;opponentReady:boolean;expiresAt:string;gameId?:string;isCreator?:boolean;isOpponent?:boolean;canAccept?:boolean;canCancel?:boolean};
export type BattleSummary={id:string;status:'setup'|'battle'|'complete';opponent:PublicIdentity;yourTurn:boolean;winner?:'you'|'opponent';updatedAt:string;challengeCode?:string};
export type RatingResult={before:number;after:number;delta:number;rank:string};
export type PvpHistoryItem={id:string;opponent:PublicIdentity;result:'victory'|'defeat';shots:number;ratingDelta:number;completedAt:string;shareId:string};
export type LeaderboardEntry={position:number;login:string;name:string;avatarUrl:string;rank:string;rating:number;wins:number;games:number;winRate:number};
export type Battles={yourTurn:BattleSummary[];waiting:BattleSummary[];finished:BattleSummary[];challenges:Challenge[]};
export type OpenHarbour={owner:PublicIdentity;periodStart:string;fleetCapacity:number;updatedAt:string;cells?:Day[];deployment?:Array<DeploymentChoice&{alive?:boolean}>};
export type AsyncBattleSummary={id:string;status:'battle'|'complete';role:'challenger'|'defender';opponent:PublicIdentity;yourTurn:boolean;winner?:'you'|'opponent';myUnitsAlive:number;opponentUnitsAlive:number;updatedAt:string};
